# Build context excludes for the frontend and backend images.
#
# Everything those Dockerfiles copy stays: the package manifests, backend/,
# frontend/, tools/ and database/prod-initdb.d.

.git
.github
.claude

**/node_modules
**/__pycache__
.pytest_cache
**/build
**/dist
**/coverage

e2e
docs
assets

# Anchored: only the screenshots at the repository root. The frontend's icons
# live under frontend/public and are copied into the image.
/*.png
*.md
LICENSE

.env
.env.*
